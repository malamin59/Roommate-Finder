import React from 'react';

const Loading = () => {
    return (
        <div className='lg:mt-[12%] mt-52 flex justify-center items-center'>
            <span className="loading loading-spinner loading-lg"></span>;
        </div>
    );
};

export default Loading;